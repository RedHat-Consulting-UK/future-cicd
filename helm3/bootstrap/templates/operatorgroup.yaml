---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.project_namespace | quote}}
  namespace: {{ .Values.project_namespace | quote}}
  annotations:
    "helm.sh/hook-weight": "1"
spec:
  targetNamespaces:
    - {{ .Values.project_namespace | quote}}